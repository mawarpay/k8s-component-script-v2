grafana:
  adminPassword: "devadmin"
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts: 
      - grafana-dev.mawarpay.com
  persistence:
    enabled: true
    storageClassName: "gp3-auto"
    size: 5Gi

prometheus:
  prometheusSpec:
    retention: 7d
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: "gp3-auto"
          resources:
            requests:
              storage: 20Gi
